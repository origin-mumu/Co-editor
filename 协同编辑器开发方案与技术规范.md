# 基于 DOM 渲染的简单协同编辑器 —— 架构方案与工程技术规范

> 本方案严格对标《全栈工程师 ｜ AI & 工程创新方向》考核要求，紧扣招聘方所看重的 **Loop 思维**（问题 → 分析 → 方案 → 实现 → 验证 → 优化）、**工程化意识**与**并发/网络一致性设计**。旨在打造一个架构清晰、代码可控、能经受住面试官深度追问的高水准全栈作品。

---

## 目录
1. [项目背景与核心目标剖析](#一-项目背景与核心目标剖析)
2. [技术选型与整体架构设计](#二-技术选型与整体架构设计)
3. [核心数据结构设计（回答追问 1）](#三-核心数据结构设计回答追问-1)
4. [协同并发仲裁与冲突处理（回答追问 2、4、5）](#四-协同并发仲裁与冲突处理回答追问-245)
5. [网络异常自愈与断网重连（回答追问 3）](#五-网络异常自愈与断网重连回答追问-3)
6. [WebSocket 通信协议全景规范](#六-websocket-通信协议全景规范)
7. [工程目录结构与编码规范](#七-工程目录结构与编码规范)
8. [7天敏捷迭代路线图（Loop 闭环实践）](#八-7天敏捷迭代路线图loop-闭环实践)
9. [高分 README 模版与答辩指南](#九-高分-readme-模版与答辩指南)

---

## 一、 项目背景与核心目标剖析

### 1.1 考核本质拆解
* **避免误区**：切忌无脑引入庞大的黑盒第三方库（如直接引入完整 Yjs、ShareDB、Tiptap、ProseMirror 插件包）。这种做法往往无法向面试官讲透内部的并发控制、网络恢复和数据结构设计原理。
* **破局要点**：
  * **自主可控**：用原生 DOM + 自定义 Block 结构 + 原生 WebSocket 实现协同流转。
  * **Loop 思维闭环**：不仅“做完功能”，更要在每个环节展示“发现瓶颈/异常 → 假设与分析 → 架构选型 → 编码实现 → 测试验证”的完整逻辑链条。
  * **工程韧性**：重点演示网络抖动、并发写冲突、断网重连、重复消息幂等处理。

### 1.2 核心功能指标
* **基础能力（必保）**：
  * 基于 DOM 渲染的 Block 文本编辑器（支持输入、修改、回车新建行、退格删除行）。
  * 双浏览器窗口同时打开，毫秒级跨窗口内容同步。
* **进阶亮点（加分必争）**：
  * 多人在线状态展示（Presence）与彩色光标位置感知。
  * Block 级别编辑租约（Focus Lease）与高亮协同框。
  * 事务（Transaction）机制与服务端操作幂等防重（Idempotency）。
  * CAS 版本号并发冲突仲裁（First Write Wins + 拒绝重拉）。
  * 客户端 Pending 队列乐观更新与断网重连自愈。

---

## 二、 技术选型与整体架构设计

```
┌────────────────────────────────────────────────────────┐
│                   Client A (React 18)                  │
│  ┌──────────────┐   ┌───────────────┐   ┌────────────┐ │
│  │ BlockEditor  │ ◄─┤ Optimistic UI │ ◄─┤ StateStore │ │
│  └──────┬───────┘   └───────────────┘   └─────▲──────┘ │
│         │               Pending Tx            │        │
│         └───────────────────┬─────────────────┘        │
└─────────────────────────────┼──────────────────────────┘
                              │ WS (Apply Tx / Reconnect)
                              ▼
┌────────────────────────────────────────────────────────┐
│               Server (Node.js + Fastify + ws)          │
│  ┌──────────────────────────────────────────────────┐  │
│  │   WebSocket Gateway (Heartbeat / Room Manager)   │  │
│  └──────────────────────────┬───────────────────────┘  │
│                             │                          │
│  ┌──────────────────────────▼───────────────────────┐  │
│  │          Single-Threaded Event Loop (FIFO)       │  │
│  │   - Idempotency Cache (TxId 去重)                 │  │
│  │   - Versioned CAS Conflict Arbiter (版本号仲裁)    │  │
│  │   - Presence & Lease Manager (块编辑租约)         │  │
│  └──────────────────────────┬───────────────────────┘  │
│                             │                          │
│  ┌──────────────────────────▼───────────────────────┐  │
│  │              In-Memory DocStore                  │  │
│  │      (支持定时导出 JSON 快照 Snapshot 持久化)     │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────┬──────────────────────────┘
                              │ Broadcast (ACK / Remote OP)
                              ▼
┌────────────────────────────────────────────────────────┐
│                   Client B (React 18)                  │
│       接收 Broadcast 增量变更，局部更新指定 Block DOM   │
└────────────────────────────────────────────────────────┘
```

### 选型矩阵与考量依据
| 层次 | 选用技术 | 替代方案 | 选型权衡与优势 |
| :--- | :--- | :--- | :--- |
| **前端视图** | **React 18 + TypeScript + Vite** | Vue 3 / 原生 JS | 声明式状态驱动 DOM 渲染极其精准，易于做 Block 粒度局部重绘，TS 提供强类型保障。 |
| **样式与交互** | **Tailwind CSS + Lucide Icons** | 手写 CSS | 开发速度极快，自带现代化美观协作 UI（彩色光标气泡、在线状态徽章）。 |
| **后端运行环境**| **Node.js (TypeScript) + `ws`** | Go / Java Spring | 前后端完全共享一份 Type 契约（零重复声明）；Node 单线程事件循环天然具备事务排队能力。 |
| **通信机制** | **原生 WebSocket (RFC 6455)** | Socket.io / HTTP 轮询 | 去除黑盒多余包装，完全掌控连接生命周期、心跳与二进制/JSON 解析，面试官高度认可。 |
| **持久化存储** | **内存 DocStore + JSON Snapshot**| Redis / MySQL | 足够轻量、单机开箱即用，代码透明度高；可随时无缝切入 Redis / SQLite。 |

---

## 三、 核心数据结构设计（回答追问 1）

> **面试追问 1：为什么选择这个数据结构？为什么是 Block，为什么不是整篇纯文本？**

### 3.1 核心数据模型（TypeScript 共享包）
```typescript
// packages/shared/src/types.ts

/**
 * 块类型枚举
 */
export type BlockType = 'paragraph' | 'heading' | 'todo';

/**
 * 单个块的数据实体
 */
export interface Block {
  id: string;             // 块唯一标识 (UUID/Nanoid)
  type: BlockType;        // 块展示样式/类型
  content: string;        // 块内纯文本
  version: number;        // 块级原子版本号 (从 1 开始递增)
  updatedAt: number;      // 更新时间戳
  updatedBy: string;      // 最后修改者 clientId
}

/**
 * 文档完整状态
 */
export interface DocumentState {
  docId: string;
  version: number;        // 文档全局版本号 (任意 Block 变动都会自增)
  blocks: Block[];        // 有序块列表
  createdAt: number;
  updatedAt: number;
}

/**
 * 协作者在线状态与光标位置
 */
export interface UserPresence {
  clientId: string;
  username: string;
  color: string;          // 随机分配的高对比度光标颜色
  activeBlockId: string | null; // 当前正在编辑/聚焦的块 ID
  cursorOffset: number;   // 光标在当前块中的字符偏移
  selectionLength: number;// 选区长度 (0 表示普通光标)
  lastSeen: number;       // 最后活跃时间
}
```

### 3.2 架构设计论据（面试答辩要点）
1. **冲突域物理隔离**：
   * 若采用传统**单一长字符串**，文档前部的任何一个插入/删除字符操作，都会导致后续所有字符的 Index 发生偏移。在并发协同下必须做复杂的字符级变换（OT 算法）。
   * 采用 **Block-based 结构**，每个段落是一个独立单元。用户 A 修改第 1 块，用户 B 修改第 5 块，两者的冲突概率为 0，服务端直接按 `blockId` 原子更新，互不干扰。
2. **DOM 渲染性能优化**：
   * React 只需为变动的 Block 进行重绘，结合 `key={block.id}` 最小化 DOM 操作，杜绝整页闪烁与光标重置。
3. **版本控制粒度细化**：
   * 每个 Block 拥有独立的 `version`，支持**块级 CAS 并发控制**，冲突仲裁只影响产生碰撞的单个段落，而不破坏整篇文档。

---

## 四、 协同并发仲裁与冲突处理（回答追问 2、4、5）

> **面试追问 2：两个人同时修改怎么办？（A: Hello → Hello World, B: Hello → Hello AI，最终内容是什么？为什么？）**  
> **面试追问 4：重复发送怎么办？（超时重发 Tx，没有收到 ACK，服务器怎么避免执行两次？）**  
> **面试追问 5：如果不用 OT / CRDT 怎么办？（Block Lock, Version, CAS, First Write Wins, Reject, Retry...）**

### 4.1 方案对比与技术取舍
在不引入高复杂度 OT/CRDT 前提下，常见的 4 种无锁/锁并发方案：
1. **Document Lock（整篇文档排他锁）**：一人编辑全篇锁定，体验极差，否决。
2. **Last Write Wins (LWW，最后写入为准)**：完全基于时间戳覆盖，极易发生静默覆盖丢数据，否决。
3. **Block Focus Lease（块级焦点意图锁）**：用户光标聚焦某块即获得 3~5 秒租约，其他端置灰。体验友好，能消灭 90% 冲突，作为**交互层防线**。
4. **Versioned CAS (Compare-And-Swap) + First Write Wins（版本仲裁）**：底层基于乐观并发控制，先到达者生效，后到达者拒绝并回滚/另起一行。作为**核心数据安全防线**。

**本项目的最终架构：`Block Focus Lease` (交互引导) + `Versioned CAS` (数据兜底仲裁)。**

---

### 4.2 冲突仲裁全流程时序图（回答追问 2 & 5）

```
[Client A]           [Server (Single Source of Truth)]        [Client B]
    │                                │                            │
    │  Block 1 初始状态: content="Hello", version=1              │
    ├────────────────────────────────┼────────────────────────────┤
    │ 本地输入 "Hello World"          │                            │ 本地输入 "Hello AI"
    │ 乐观更新 UI                     │                            │ 乐观更新 UI
    │ 发送 Tx1(baseVer: 1)           │                            │ 发送 Tx2(baseVer: 1)
    │───────────────────────────────>│                            │
    │                                │ 先到达!                    │
    │                                │ 检查: baseVer(1) == cur(1) │
    │                                │ 成功! version 升为 2       │
    │                                │ content="Hello World"      │
    │<────── ACK(txId1, ver: 2) ─────┤                            │
    │                                │                            │
    │                                │ 后到达!                    │
    │                                │ 检查: baseVer(1) != cur(2) │
    │                                │ 触发冲突!                   │
    │                                │ 拒绝执行 Tx2               │
    │                                │───────────────────────────>│
    │                                │  REJECT_TX(txId2, ver: 2,  │
    │                                │  latestContent:            │
    │                                │  "Hello World")            │
    │                                │                            │
    │                                │                            │ 收到 Reject:
    │                                │                            │ 1. 回滚当前块为最新值
    │                                │                            │ 2. 弹出轻提示提醒用户
    │                                │                            │ 3. 允许自动插入新行保留
    │                                │                            │    B 未保存的内容
    │                                │                            │
    │                                │ 广播变更                   │
    │                                ├───────────────────────────>│
    │                                │ BROADCAST_OP(ver: 2)       │
```

#### 仲裁结论回答：
* **最终内容**：Block 1 内容确定为 `"Hello World"`（版本 2）。
* **为什么**：服务端事件循环按物理到达顺序仲裁，A 先提交且版本匹配（CAS 成功）；B 的事务基于已过期的旧版本（baseVersion=1），被服务端依据 First Write Wins 规则判定失效并拒绝，避免了脏写覆盖，保证了分布式状态的最终一致性。

---

### 4.3 幂等性与防重复执行机制（回答追问 4）

```typescript
// packages/server/src/idempotency.ts

export interface TxExecutionResult {
  txId: string;
  success: boolean;
  resultingVersion: number;
  timestamp: number;
}

export class IdempotencyManager {
  // 维护滑动窗口缓存：记录近 5 分钟内已处理过的事务 ID
  private processedTxs: Map<string, TxExecutionResult> = new Map();
  private readonly TTL = 5 * 60 * 1000;

  public hasExecuted(txId: string): boolean {
    this.cleanExpired();
    return this.processedTxs.has(txId);
  }

  public getResult(txId: string): TxExecutionResult | undefined {
    return this.processedTxs.get(txId);
  }

  public recordSuccess(txId: string, version: number) {
    this.processedTxs.set(txId, {
      txId,
      success: true,
      resultingVersion: version,
      timestamp: Date.now()
    });
  }

  private cleanExpired() {
    const now = Date.now();
    for (const [txId, item] of this.processedTxs.entries()) {
      if (now - item.timestamp > this.TTL) {
        this.processedTxs.delete(txId);
      }
    }
  }
}
```
* **解决重发机制**：
  1. 客户端发送事务时，生成全局唯一 `txId = ${clientId}_${counter}_${nanoid()}`，并开启 3 秒的 ACK 等待计时器；
  2. 若因网络抖动未收到 ACK，客户端自动触发重试；
  3. 服务端收到请求时先经过 `IdempotencyManager`：
     * **若已处理过**：直接返回已记录的 ACK 成功包，**绝对不重复执行数据修改与自增**；
     * **若未处理过**：正常走 CAS 修改逻辑，并写入去重缓存。

---

## 五、 网络异常自愈与断网重连（回答追问 3）

> **面试追问 3：网络断开怎么办？（修改 → 本地已经显示 → WebSocket 断了 → 服务器没有收到，你怎么恢复？）**

### 5.1 客户端状态机与断线检测
客户端封装四态状态机：
* `CONNECTED`：常规心跳与双向同步中；
* `DISCONNECTED`：检测到底层 WS `onclose` 或心跳超时（3 次 Ping 无 Pong 回复）；
* `RECONNECTING`：启动指数退避重连（1s → 2s → 4s → 最大 10s）；
* `SYNCING`：连接重新建立，正在与服务端版本进行 Diff 对齐。

```
   [CONNECTED] ──────── (心跳超时/连接断开) ───────► [DISCONNECTED]
        ▲                                                │
        │                                                ▼
   (版本对齐完毕)                                    [RECONNECTING]
        │                                                │
   [SYNCING] ◄──────── (WebSocket 重新握手成功) ─────────┘
```

### 5.2 本地未确认队列（Pending Queue）与状态自愈
```typescript
// packages/client/src/hooks/useEditorSync.ts

interface PendingTransaction {
  tx: TransactionPayload;
  retryCount: number;
  timestamp: number;
}

// 客户端状态管理
class ClientSyncManager {
  private pendingQueue: PendingTransaction[] = [];

  // 用户击键时
  public onUserEdit(blockId: string, newContent: string) {
    // 1. 乐观更新本地 UI (保证打字丝滑无卡顿)
    this.applyLocalOptimisticUpdate(blockId, newContent);

    // 2. 构造事务包
    const tx = this.createTx(blockId, newContent);

    // 3. 推入未确认队列
    this.pendingQueue.push({ tx, retryCount: 0, timestamp: Date.now() });

    // 4. 若在线则发送，若离线则留存
    if (this.isOnline()) {
      this.sendTx(tx);
    }
  }

  // 重连成功后的三步恢复闭环
  public async onReconnected(lastKnownDocVersion: number) {
    // 第一步：向服务端发起对齐请求
    const syncRes = await this.requestSync({ docVersion: lastKnownDocVersion });

    // 第二步：如果离线期间服务端已有新版本更新，下发最新全量 Snapshot 覆盖本地基准
    if (syncRes.type === 'FULL_SNAPSHOT') {
      this.applyServerSnapshot(syncRes.snapshot);
    }

    // 第三步：重放本地 pendingQueue 中尚未被 ACK 确认的事务
    while (this.pendingQueue.length > 0) {
      const item = this.pendingQueue.shift()!;
      // 更新其 baseVersion 为当前基准，尝试重新提交
      item.tx.baseVersion = this.getCurrentBlockVersion(item.tx.blockId);
      this.sendTx(item.tx);
    }
  }
}
```

---

## 六、 WebSocket 通信协议全景规范

前后端基于 JSON 报文传输，统一外层 Envelope：

```typescript
export interface WSEnvelope<T = any> {
  action: WSAction;
  docId: string;
  clientId: string;
  timestamp: number;
  payload: T;
}

export enum WSAction {
  // 1. 房间与握手
  JOIN_ROOM = 'JOIN_ROOM',             // C -> S: 进入文档
  INIT_STATE = 'INIT_STATE',           // S -> C: 下发文档全量快照与当前协作者
  
  // 2. 事务流
  APPLY_TX = 'APPLY_TX',               // C -> S: 提交修改事务
  ACK_TX = 'ACK_TX',                   // S -> C: 事务确认成功
  REJECT_TX = 'REJECT_TX',             // S -> C: 事务冲突/校验失败拒绝
  BROADCAST_OP = 'BROADCAST_OP',       // S -> C (其他客户端): 增量操作广播
  
  // 3. 协同感知 (Presence)
  PRESENCE_UPDATE = 'PRESENCE_UPDATE', // C -> S: 上报当前光标位置与焦点 Block
  PRESENCE_BROADCAST = 'PRESENCE_BROADCAST', // S -> C: 广播协作者光标与状态
  
  // 4. 网络恢复
  SYNC_REQUEST = 'SYNC_REQUEST',       // C -> S: 重连后请求差异对齐
  SYNC_RESPONSE = 'SYNC_RESPONSE',     // S -> C: 差异对齐结果
  
  // 5. 心跳保活
  PING = 'PING',
  PONG = 'PONG'
}
```

### 典型报文示例
#### ① 客户端提交修改（APPLY_TX）
```json
{
  "action": "APPLY_TX",
  "docId": "doc-default",
  "clientId": "client-a-8f92",
  "timestamp": 1727092800000,
  "payload": {
    "txId": "tx-client-a-8f92-101",
    "blockId": "block-uuid-1",
    "opType": "UPDATE_BLOCK",
    "baseVersion": 1,
    "content": "Hello World"
  }
}
```

#### ② 服务端响应确认（ACK_TX）
```json
{
  "action": "ACK_TX",
  "docId": "doc-default",
  "clientId": "client-a-8f92",
  "timestamp": 1727092800025,
  "payload": {
    "txId": "tx-client-a-8f92-101",
    "blockId": "block-uuid-1",
    "newVersion": 2,
    "success": true
  }
}
```

#### ③ 服务端广播增量更新给其他协作者（BROADCAST_OP）
```json
{
  "action": "BROADCAST_OP",
  "docId": "doc-default",
  "clientId": "client-a-8f92",
  "timestamp": 1727092800025,
  "payload": {
    "opType": "UPDATE_BLOCK",
    "blockId": "block-uuid-1",
    "content": "Hello World",
    "version": 2,
    "docVersion": 15,
    "updatedBy": "client-a-8f92"
  }
}
```

---

## 七、 工程目录结构与编码规范

### 7.1 Monorepo 项目结构
```text
Co-editor/
├── packages/
│   ├── shared/                 # 共享协议层 (严格类型守卫)
│   │   ├── src/
│   │   │   ├── index.ts        # 导出出口
│   │   │   ├── types.ts        # Block, Document, Presence 核心定义
│   │   │   ├── protocol.ts     # WSAction 与消息 Envelope
│   │   │   └── utils.ts        # 通用 ID 生成与校验工具
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── server/                 # 后端 WebSocket 与状态服务
│   │   ├── src/
│   │   │   ├── index.ts        # 入口与端口监听
│   │   │   ├── wsServer.ts     # 原生 WebSocket 连接生命周期
│   │   │   ├── roomManager.ts  # 多文档房间调度
│   │   │   ├── documentStore.ts# 内存文档树与持久化 Snapshot
│   │   │   ├── idempotency.ts  # 幂等防重缓存
│   │   │   └── presence.ts     # 协作者状态与租约管理
│   │   ├── test/               # 并发仲裁单测
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── client/                 # 前端 React 协作编辑器
│       ├── src/
│       │   ├── components/
│       │   │   ├── Editor.tsx       # 编辑器根组件
│       │   │   ├── BlockItem.tsx    # 单块渲染、聚焦、按键处理
│       │   │   ├── PresenceBar.tsx  # 顶部协作者在线列表
│       │   │   ├── RemoteCursor.tsx # 远程协作者光标与高亮框
│       │   │   └── NetworkBadge.tsx # 网络状态指示器 (在线/离线/重连中)
│       │   ├── hooks/
│       │   │   ├── useWebSocket.ts  # 网络状态机与心跳 Hook
│       │   │   └── useEditorSync.ts # 乐观更新、Pending 队列、ACK 处理
│       │   ├── App.tsx
│       │   ├── main.tsx
│       │   └── index.css
│       ├── package.json
│       ├── vite.config.ts
│       └── tsconfig.json
├── package.json                # npm/pnpm workspace 统一管理
├── README.md                   # 面试提交报告 (按考核要求撰写)
├── 代码规范与工程准则.md         # 详细代码规范（文件行数、模块拆分、TS约束等）
└── 协同编辑器开发方案与技术规范.md # 本方案文档
```

### 7.2 编码规范与关键红线（详见《代码规范与工程准则.md》）
1. **单文件规模严格限制（LOC ≤ 500 行）**：
   * 任何 `.ts` / `.tsx` 文件长度严禁超过 500 行，推荐维持在 150~300 行；
   * 业务膨胀时必须及时进行横向拆分（如抽离私有 Helper、拆分按键分发器 `handleKeyNavigation.ts`、将 WS 连接与状态同步拆分为独立 Hook）。
2. **函数体与嵌套深度**：单个函数不超过 50 行，JSX 嵌套不超过 4 层，函数参数不超过 3 个（超过封装为 Options 对象）。
3. **TypeScript 严格类型，零 `any`**：必须开启 `"strict": true`，禁止使用 `: any`，网络数据传输反序列化必须进行 Type Guard 类型守卫断言。
4. **不可变数据流（Immutability）**：禁止直接原地修改 `block.content` 或数组，保证 React 浅比对高效触发。
5. **UI 组件哑化**：视图组件严禁直接调用 `ws.send`，统一通过回调通知 Hook 处理。
6. **结构化日志与异常防护**：严禁静默吞掉异常，统一日志标签格式如 `[WS:CONNECT]`、`[CAS:APPLY]`、`[IDEM:HIT]`。

---

## 八、 7天敏捷迭代路线图（Loop 闭环实践）

贯彻“**问题 → 分析 → 方案 → 实现 → 验证 → 优化**”闭环：

| 阶段 | 核心目标 | 详细交付任务 | 验证闭环场景（Loop 证明） |
| :--- | :--- | :--- | :--- |
| **Day 1** | **契约先行与单机 Block 骨架** | 1. 初始化 Monorepo 与 shared 类型库。<br>2. 实现 React BlockList + BlockItem。<br>3. 跑通原生 Enter 换行、Backspace 删行逻辑。 | **验证**：光标移动流畅，段落增删无错位，DOM 层次清爽。 |
| **Day 2** | **双端网络打通与广播同步** | 1. 搭建 Node.js `ws` 服务端。<br>2. 建立基础连接，广播全量与增量 Block 更新。 | **验证**：两个浏览器窗口并排，A 打字，B 毫秒级同步显示内容。 |
| **Day 3** | **事务 ACK 与幂等防重机制** | 1. 实现 `txId` 生成与客户端乐观更新。<br>2. 服务端实现 `IdempotencyManager`。<br>3. 客户端实现超时未 ACK 自动重试。 | **验证**：通过 Postman/脚本连续重发同一 `txId` 10 次，服务端只执行 1 次，返回相同 ACK。 |
| **Day 4** | **协同感知与彩色远程光标** | 1. 实现 `Presence` 协议与心跳定时器。<br>2. 客户端广播光标位置与 Block 焦点。<br>3. 渲染协作者头像列表与高亮编辑边框。 | **验证**：A 点击第 2 段，B 窗口该段落立即显示 A 的专属颜色与“XXX 正在编辑”标签。 |
| **Day 5** | **CAS 版本仲裁与并发冲突回滚** | 1. 每个 Block 引入 `version` 字段。<br>2. 服务端实现 CAS 冲突校验。<br>3. 客户端接收 `REJECT_TX` 并优雅提示回滚。 | **验证**：编写测试脚本模拟 A、B 同时提交 version=1 的修改，校验只有 1 个成功，另 1 个被拒绝且数据完整。 |
| **Day 6** | **断网自愈与 Pending 队列重放** | 1. 客户端封装断线重连状态机。<br>2. 离线时保存未确认变更至 `pendingQueue`。<br>3. 重连后自动执行三步握手同步。 | **验证**：在 Chrome DevTools 切换为 Offline，编辑数个段落，切回 Online，验证数据全部自动补齐上报。 |
| **Day 7** | **工程打磨、录屏与 README 交付** | 1. 完善磁盘 Snapshot 快照落盘。<br>2. 撰写标准化作品 README。<br>3. 录制 1~2 分钟双端协同演示视频或动图。 | **验证**：执行一键启动命令，全流程无任何控制台 Warning/Error。 |

---

## 九、 高分 README 模版与答辩指南

在提交项目时，请严格按以下 7 项骨架撰写项目 `README.md`，直接贴合考核点：

```markdown
# Co-editor 协同编辑器

> 面试作品：基于 DOM 渲染与 Block 架构的轻量级协同编辑器  
> 开发者：[你的姓名] ｜ 目标岗位：全栈工程师（AI & 工程创新方向）

## 1. 快速启动指南
- 环境要求：Node.js >= 18, pnpm >= 8
- 安装依赖：pnpm install
- 一键启动：pnpm dev （同时启动 Server:8080 与 Client:5173）
- 协同体验：同时在两个浏览器标签页访问 http://localhost:5173

## 2. 技术选型与权衡
- 为什么选用 Block 架构而非单一大文本？（阐明冲突隔离与 DOM 性能）
- 为什么选用原生 WebSocket？（阐明协议轻量可控与状态机透明度）

## 3. 核心数据结构设计
- 贴出 Block, DocumentState, UserPresence 结构体
- 说明 version、txId 字段在并发仲裁中的作用

## 4. 协同机制与冲突处理
- 协同架构流程图
- 5 大关键追问的解答（CAS 仲裁、幂等防重、断网恢复）

## 5. 开发过程中遇到的问题与 Loop 闭环优化
- 案例 1：React 输入中文 IME 状态与协同覆盖冲突的解决
- 案例 2：断线重连后并发写造成服务端队列拥塞的防抖合并优化

## 6. 当前局限性（客观剖析）
- 当前仅支持文本块，尚未支持块与块之间的拖拽排序
- 块内并发冲突策略为 First Write Wins，尚未支持行内字符级 Diff 合并

## 7. 下一步演进规划
- 引入 LWW-Element-Set（CRDT）解决 Block 树状层级与拖拽重排
- 结合 AI Agent（如输入 /ai 触发大模型自动补全或重写指定 Block）
```

---

## 十、 针对招聘方 5 大灵魂提问的标准答辩参考速查

| 招聘方提问 | 核心技术答案要点 |
| :--- | :--- |
| **为什么选择这个数据结构？** | 采用有序 Block 列表结构，将整篇文档的冲突域物理隔离到单个 Block，天然消灭 80% 的跨段落并发冲突；同时 Block 独立版本号让 CAS 粒度极小。 |
| **两个人同时修改同一个 Block 怎么办？** | 服务端事件循环单线程串行处理。先到者版本匹配执行成功（CAS 成功），版本自增为 2；后到者基于旧版本提交触发冲突拒绝（First Write Wins），客户端回滚并提示拉取最新。 |
| **网络断开怎么办？** | 客户端状态机进入 RECONNECTING，离线输入进行乐观更新并缓存在本地 `pendingQueue`；重连成功后发起版本差异同步，拉取最新快照并重放未确认事务。 |
| **重复发送怎么办？** | 客户端生成全局唯一 `txId`；服务端维护滑动窗口 `IdempotencyManager`，已处理过的 `txId` 直接返回已记录的 ACK 成功包，跳过重复业务执行。 |
| **如果不用 OT / CRDT 怎么办？** | 可以采用 **Block Focus Lease（块级焦点意图锁）** 结合 **Versioned CAS（版本校验）**，以最低的系统复杂度实现无脏写的最终一致性。 |

---
*本文档为项目标准工程规范，后续所有代码实现与测试用例均基于此规范开展。*
