# 协同编辑器项目 —— 详细代码规范与工程开发准则

> 本规范旨在保障项目代码具备**高内聚、低耦合、严谨健壮、易于重构与答辩演示**的工程质量。所有前后端开发必须严格遵守本准则。

---

## 目录
1. [代码规模与模块拆分规范（500行上限）](#一-代码规模与模块拆分规范500行上限)
2. [架构分层与职责边界规范](#二-架构分层与职责边界规范)
3. [TypeScript 严格类型规范](#三-typescript-严格类型规范)
4. [命名与代码风格规范](#四-命名与代码风格规范)
5. [前端 React 组件与 Hook 规范](#五-前端-react-组件与-hook-规范)
6. [后端状态机与并发操作规范](#六-后端状态机与并发操作规范)
7. [错误处理与结构化日志规范](#七-错误处理与结构化日志规范)
8. [Git 提交与变更规范](#八-git-提交与变更规范)

---

## 一、 代码规模与模块拆分规范（500行上限）

为了避免出现难以维护的“上帝文件”（God File），强制执行以下规模限制：

| 维度 | 硬性上限 | 推荐目标 | 超标处置方案 |
| :--- | :--- | :--- | :--- |
| **单文件行数 (LOC)** | **≤ 500 行** | **150 ~ 300 行** | 超过 300 行必须评估拆分；超过 500 行禁止合并，必须拆解为子模块或 Hook。 |
| **单函数/方法行数** | **≤ 50 行** | **15 ~ 30 行** | 拆分为具名私有辅助函数（Private Helper Function）或流水线步骤。 |
| **组件 JSX 嵌套层级** | **≤ 4 层** | **≤ 3 层** | 抽离局部子组件（如 `BlockHandle`、`BlockGutter`、`CursorTooltip`）。 |
| **函数参数个数** | **≤ 3 个** | **1 ~ 2 个** | 超过 3 个参数必须重构为 Options 对象或配置上下文（Config Object）。 |

### 500行超标典型拆分策略：
1. **庞大组件拆分**：
   * `Editor.tsx` 只做容器布局；
   * 键盘事件（Enter、Backspace、ArrowUp/Down）抽离为独立的按键分发器 `handleKeyNavigation.ts`；
   * 远程光标渲染抽离为 `RemoteCursorLayer.tsx`。
2. **复合 Hook 拆分**：
   * 不要将 WebSocket 连接与编辑器业务揉在同一个 Hook 里；
   * 拆分为底层网络 Hook `useWebSocket.ts`（只负责连接与心跳）与业务协同 Hook `useEditorSync.ts`（负责 Tx 队列、版本与乐观更新）。
3. **服务端模块解耦**：
   * `wsServer.ts` 只负责网络报文解包；
   * 具体的 CAS 并发比对移交 `documentStore.ts`；
   * 事务防重逻辑剥离到 `idempotency.ts`。

---

## 二、 架构分层与职责边界规范

项目划分为三层物理架构，各层之间单向依赖，严禁循环引用：

```
┌────────────────────────────────────────────────────────┐
│            packages/shared (协议层与纯逻辑)             │
│   - 纯类型定义 (types.ts)                               │
│   - 纯常量与协议动作枚举 (protocol.ts)                  │
│   - 纯工具函数 (无 DOM、无 Node.js API)                │
└───────────────────▲─────────────────▲──────────────────┘
                    │                 │
           依赖共享 ├──               ──┤ 依赖共享
                    │                   │
┌───────────────────┴────┐      ┌───────┴────────────────┐
│ packages/client (前端) │      │ packages/server (后端) │
│ - UI 渲染层 (Views)    │      │ - 网络网关层 (WS)      │
│ - 协同状态层 (Hooks)   │      │ - 业务仲裁层 (CAS)     │
│ - 本地缓存层 (Pending) │      │ - 内存存储层 (Store)   │
└────────────────────────┘      └────────────────────────┘
```

### 职责边界红线：
1. **`shared/` 红线**：绝对禁止引入任何前端专有依赖（如 `react`、`window`、`document`）或后端专有依赖（如 `fs`、`ws`、`path`）。它必须是跨端完全中立的纯 TypeScript 代码。
2. **UI 组件哑化红线**：视图组件（如 `BlockItem.tsx`）严禁直接调用 `ws.send()`。所有网络行为必须经由回调向上传递给协同 Hook 统一发号施令。
3. **状态单一可信源红线**：服务端内存中的 `DocumentState` 是文档状态的唯一真理；客户端的显示变更一律标记为“乐观态”，直到收到服务端带版本号的 `ACK` 方转为“确认态”。

---

## 三、 TypeScript 严格类型规范

必须在各子项目的 `tsconfig.json` 中开启严格模式：`"strict": true`。

### 1. 绝不允许使用 `any`
* 严禁在代码中出现 `: any`。如果不确定类型，使用 `unknown` 并配合类型守卫（Type Guard）窄化类型。
```typescript
// ❌ 错误示范
function handleMessage(msg: any) {
  console.log(msg.payload.content);
}

// ✅ 正确示范
function isTxPayload(payload: unknown): payload is TxPayload {
  return typeof payload === 'object' && payload !== null && 'txId' in payload;
}

function handleMessage(envelope: WSEnvelope<unknown>) {
  if (isTxPayload(envelope.payload)) {
    console.log(envelope.payload.content);
  }
}
```

### 2. 避免使用类型断言 `as`
* 除非在极少数初始化场景，尽量依靠 TS 自动类型推导，不滥用 `as T` 强转。

### 3. 类型与接口定义准则
* 优先使用 `interface` 定义对象实体结构（如 `Block`、`UserPresence`）；
* 联合类型、元组或映射类型使用 `type`（如 `type BlockType = 'paragraph' | 'heading' | 'todo'`）；
* 接口命名禁止加冗余的前缀 `I`（用 `Block` 而非 `IBlock`）。

---

## 四、 命名与代码风格规范

### 1. 命名约定
| 对象类型 | 命名规则 | 示例 |
| :--- | :--- | :--- |
| **React 组件** | 大驼峰（PascalCase） | `BlockItem.tsx`, `PresenceBar.tsx` |
| **自定义 Hook** | 小驼峰（以 `use` 开头） | `useWebSocket.ts`, `useEditorSync.ts` |
| **工具类 / 模块** | 小驼峰（camelCase） | `idempotency.ts`, `diffUtils.ts` |
| **常量 / 枚举成员**| 全大写下划线（CONSTANT_CASE） | `MAX_RETRY_COUNT`, `WSAction.APPLY_TX` |
| **TypeScript 类型**| 大驼峰（PascalCase） | `DocumentState`, `UserPresence` |
| **布尔变量** | 助动词前缀（is/has/can/should）| `isOnline`, `hasConflict`, `canEdit` |

### 2. 格式与空白排版
* 缩进：统一使用 **2 个空格**，严禁使用 Tab。
* 句尾分号：**保留分号**（避免 ASI 自动插入分号带来的隐患）。
* 单行字符数：建议在 **100 字符**以内换行，最长不超过 **120 字符**。
* 导入排序：
  1. Node.js 内置模块（`node:path`, `node:events`）
  2. 第三方外部库（`react`, `ws`, `lucide-react`）
  3. 内部共享包（`@co-editor/shared`）
  4. 相对路径子模块（`../hooks/...`, `./BlockItem`）

---

## 五、 前端 React 组件与 Hook 规范

### 1. 不可变更新原则（Immutability）
严禁直接修改原有数组或对象的内部属性，必须通过纯函数返回新引用，确保 React 浅比较与 Memo 能高效工作：
```typescript
// ❌ 错误示范：原地突变
blocks[index].content = newText;
setBlocks(blocks);

// ✅ 正确示范：不可变拷贝
setBlocks(prevBlocks =>
  prevBlocks.map(block =>
    block.id === targetId ? { ...block, content: newText } : block
  )
);
```

### 2. 核心状态抽离（Hooks 分工清晰）
* `useWebSocket`：专职处理底层 WebSocket、重连定时器、心跳 Ping-Pong；
* `usePresence`：专职处理在线用户列表、自身光标位置广播与节流（Throttle 50ms）；
* `useEditorSync`：专职处理 Block 列表渲染、乐观更新、Pending 事务重放、冲突拒绝回滚。

### 3. DOM 性能与渲染防抖
* 输入框内容同步：打字时本地立即反映，向服务端发送 `APPLY_TX` 必须做**输入合并或短节流（例如 50ms~100ms）**，避免用户每敲一个字母就发送一次网络事务，造成服务端无谓的事务风暴。

---

## 六、 后端状态机与并发操作规范

### 1. 单线程事件循环防并发重入
* 虽然 Node.js 是单线程，但在处理 `async/await` 异步调用（如持久化落盘）时，上下文可能发生切换。
* **规则**：所有涉及内存 `DocumentState` 修改的 CAS 比对与版本号自增，必须在**同步代码块（Sync Function）**中一口气执行完毕，杜绝异步穿插导致版本覆盖！

```typescript
// ✅ 正确示范：同步原子变更
public applyTransaction(tx: TransactionPayload): TxResult {
  const block = this.blocksMap.get(tx.blockId);
  if (!block) return { success: false, reason: 'BLOCK_NOT_FOUND' };

  // CAS 版本比对必须在同步逻辑中执行
  if (block.version !== tx.baseVersion) {
    return { success: false, reason: 'VERSION_CONFLICT', currentVersion: block.version };
  }

  // 同步原子自增与变更
  block.content = tx.content;
  block.version += 1;
  this.docVersion += 1;

  return { success: true, newVersion: block.version };
}
```

### 2. 内存清理与防泄露机制
* `IdempotencyManager`（幂等去重）：必须设定固定容量上限（如最大 5000 条）和基于时间（如 5 分钟）的过期自动淘汰机制，防止服务器内存泄露。
* 客户端断开连接（`onclose`）：必须在 5 秒宽限期后彻底清除其对应的 `Presence` 状态并广播 `USER_LEFT`。

---

## 七、 错误处理与结构化日志规范

### 1. 严禁静默吞掉异常
所有 `try...catch` 必须显式处理异常，严禁留空：
```typescript
// ❌ 错误示范
try {
  ws.send(data);
} catch (e) {}

// ✅ 正确示范
try {
  ws.send(data);
} catch (err) {
  logger.error('[WS:SEND_FAIL]', { txId: tx.txId, error: err });
  this.pendingQueue.push(tx); // 降级入重试队列
}
```

### 2. 结构化日志前缀标准
为了方便调试、压测观察与技术复盘，统一采用分类日志前缀：

```typescript
// 格式：[模块:动作] 关键字段与上下文
console.log('[WS:CONNECT] Client connected, clientId = %s', clientId);
console.log('[CAS:APPLY] Tx applied, blockId = %s, version = %d -> %d', blockId, oldVer, newVer);
console.warn('[CAS:CONFLICT] Version mismatch! baseVer = %d, currentVer = %d', baseVer, curVer);
console.info('[IDEM:HIT] Duplicate txId ignored: %s', txId);
console.error('[SYNC:FAIL] Sync handshake failed: %s', err.message);
```

---

## 八、 Git 提交与变更规范

提交日志使用社区通用的 **Conventional Commits** 规范，展示良好的工程素养：

### 格式模板
`<type>(<scope>): <subject>`

* **type（类型）**：
  * `feat`: 新增特性（如加入在线光标、实现 CAS 仲裁）
  * `fix`: 修复 Bug（如修复断网后光标跳跃、修复连续敲击导致版本错乱）
  * `refactor`: 重构（如拆分超过 300 行的文件、抽离共享类型）
  * `perf`: 性能优化（如加入输入节流、优化 DOM 渲染）
  * `docs`: 文档变更（如编写 README、补充架构图）
  * `test`: 测试用例（如编写并发冲突单测）
* **scope（作用域）**：`client` / `server` / `shared` / `config`

### 提交示范
```bash
feat(shared): 定义 Block 与 WSMessage 核心协议接口
feat(client): 实现 BlockItem 基础 DOM 渲染与按键监听
feat(server): 实现基于单线程队列的 Versioned CAS 冲突仲裁
fix(client): 解决离线重连后未确认队列重放导致的光标错位
perf(client): 增加 50ms 击键防抖，降低无效 WS 广播频率
docs: 完成项目 README 与核心技术架构解析
```

---

## 九、 规范检查 Checklist（自检清单）

在代码提交与合并前，按此清单逐一自测：
- [ ] 检查所有 `.ts` / `.tsx` 文件，**无任何单个文件超过 500 行**。
- [ ] 全项目搜索 `: any`，**零残留**。
- [ ] 视图组件与 WebSocket 彻底解耦，无直接在 UI 内 `ws.send` 的硬编码。
- [ ] 单个函数逻辑清晰，无超过 50 行的长逻辑堆砌。
- [ ] 控制台无 React 缺失 `key` 警告与未捕获的 Uncaught Promise Error。
- [ ] 运行 `pnpm build` 或 `tsc --noEmit`，一次性编译无任何类型报错。
